# 포켓 스터디 (PokéStudy) — 개발 명세서 v1.0

> 교사가 출제한 학습 문제로 포켓몬을 배틀·포획하며 1세대 도감(151마리)을 완성하는 학급용 교육 게임 웹앱.
> 본 문서는 4단계 통합 프로토타입(`pokestudy-integrated-prototype.jsx`)까지 검증된 설계를 실전 배포용 웹앱으로 구현하기 위한 명세다.
> 프로토타입 3종(`pokemon-battle-prototype.jsx`, `teacher-menu-prototype.jsx`, `student-economy-prototype.jsx`)과 통합본이 UI/게임 로직의 참조 구현이다.

---

## 1. 프로젝트 개요

- **목적**: 학급 단위로 지속 사용하는(단발성 아님) 학습 동기 부여 게임. 교사가 문제를 관리하고, 학생은 문제를 풀어 포인트·아이템을 얻고 포켓몬을 포획하여 도감을 완성한다.
- **핵심 루프**: 문제 풀이 → 포인트/데미지 → 아이템 구매/포켓몬 포획 → 도감 수집 → 다시 문제 풀이.
- **포획 2트랙**:
  - 트랙 A (야생 탐색): 하루 3회, 배틀 없이 아이템(볼)만으로 포획. 운 중심, 하위권 학생도 소외되지 않는 장치.
  - 트랙 B (배틀): 교사가 등록한 문제를 풀어 야생 포켓몬의 HP를 깎아 쓰러뜨린 뒤 볼을 던져 포획. 실력 중심.
- **데이터 소스**: PokéAPI (https://pokeapi.co, 무료, 키 불필요). 이미지·기술·한국어 이름 등. 상업적 사용 시 응답 캐싱 권장.

## 2. 사용자 역할 및 인증

### 2.1 교사
- 이메일 또는 아이디 기반 가입(일반적인 방식).
- 가입 후 **학급 생성 → 학급 코드 자동 발급** (예: TIGER24). 코드는 학급당 고유.
- 한 교사가 여러 학급을 만들 수 있으면 좋으나 MVP에서는 1교사 1학급도 무방.

### 2.2 학생 (개인정보 최소화 원칙 — 필수)
- **학급 코드 + 닉네임 + 비밀번호(4자 이상)만으로 가입.** 이메일·실명·전화번호 등 개인정보를 일절 수집하지 않는다.
- 닉네임은 학급 내에서 고유.
- 가입 즉시 초기 지급: **500P + 몬스터볼 3개**.
- 비밀번호는 반드시 해시(bcrypt 등)하여 저장. 프로토타입의 평문 저장은 금지.
- 비밀번호 분실 대비: 교사가 관리 화면에서 학생 비밀번호를 초기화할 수 있어야 한다.

## 3. 화면 구조

```
접속 → 랜딩/로그인
 ├─ 학생 로그인 → 학생 홈 (탭 6개)
 │   ① 배틀  ② 문제풀이  ③ 오늘의 퀴즈  ④ 야생 탐색  ⑤ 상점  ⑥ 도감
 └─ 교사 로그인 → 교사 홈 (탭)
     ① 문제 은행  ② AI 문제 생성  ③ 대량 등록  ④ 통계  ⑤ 게임 설정  ⑥ 학생 관리
```

- 학생 화면 톤: 게임보이풍 픽셀 감성(어두운 남색 배경 #1a1c2c, 크림색 메시지 박스 #f8f0dc, 픽셀 폰트 예: DungGeunMo). 프로토타입 참조.
- 교사 화면 톤: 밝고 사무적인 관리 도구(흰 배경, 남색 헤더). 프로토타입 참조.
- 모바일(스마트폰) 대응 필수 — 학생은 태블릿/폰 사용 비중이 높다.

## 4. 게임 시스템 명세

### 4.1 포켓몬 데이터
- 대상: **1세대 151마리** (확장 시 2세대 해금 구조 고려).
- 희귀도 3등급:
  - 전설(5): 도감번호 144(프리져), 145(썬더), 146(파이어), 150(뮤츠), 151(뮤)
  - 희귀(26): 3, 6, 9, 25, 26, 38, 45, 59, 65, 68, 94, 113, 115, 123, 127, 130, 131, 133, 134, 135, 136, 142, 143, 147, 148, 149
  - 흔함(120): 나머지 전부
- 희귀도별 파라미터:

| 항목 | 흔함 | 희귀 | 전설 |
|---|---|---|---|
| 출현 확률 | 65% | 30% | 5% |
| 배틀 HP | 40 | 70 | 120 |
| 반격 데미지 | 10 | 15 | 25 |
| 기본 포획률(몬스터볼) | 90% | 60% | 30% |
| 승리 포인트 | +50P | +100P | +300P |
| 승리 경험치 | +20XP | +40XP | +100XP |
| 표시 레벨 | Lv.5 | Lv.12 | Lv.30 |

- 이미지: PokéAPI sprites (official-artwork = 정면, back sprite = 내 포켓몬 등짝). 서버 또는 CDN에 캐싱.
- 한국어 이름: `/pokemon-species/{id}`의 `names`에서 `language.name === "ko"`. 초기 1회 수집 후 DB에 저장(매번 API 호출 금지). 프로토타입 코드에 151마리 한국어 이름+타입 하드코딩본이 있으니 시드 데이터로 활용 가능.
- 조사(이/가, 을/를) 처리: 받침 유무로 분기 (프로토타입 `josa()` 함수 참조).

### 4.2 배틀 시스템 (트랙 B)
- **기술 선택 = 난이도 선택** (교사 설정으로 ON/OFF 가능):
  - 쉬움 기술: 위력 10 / 보통: 20 / 어려움: 35
  - OFF 시 "공격" 버튼 하나, 보통 난이도 랜덤 출제.
  - 기술 이름은 내 포켓몬의 실제 기술(PokéAPI moves, 위력 기준 3단계 자동 선발: ≤40 쉬움, 41–70 보통, ≥71 어려움). 전설·환상은 전용기를 어려움 슬롯에 우선 배치. (프로토타입은 리자몽 고정: 불꽃세례/화염방사/오버히트)
- **턴 진행**:
  1. 학생이 기술 선택 → 해당 난이도의 문제 출제 (같은 배틀 내 중복 출제 방지, 소진 시 재사용 허용)
  2. 제한시간: 쉬움 15초 / 보통 25초 / 어려움 40초
  3. 정답 → 기술 명중, 야생 HP 감소. **급소**: 제한시간의 앞 1/3 안에 정답 시 데미지 1.5배 + "급소에 맞았다!" 연출
  4. 오답·시간초과 → **"기술이 빗나갔다!"** → 턴이 넘어가 야생 포켓몬이 반격(희귀도별 반격 데미지). 오답 시 정답 공개.
  5. 정답 1개당 +2XP (승패 무관 — 져도 공부한 만큼 성장).
- **승리(야생 HP 0)** → 포획 단계: 보유한 볼 중 선택해 던짐. 볼 1개 소모.
  - 성공률 = 희귀도 기본 포획률 + 볼 보너스 (상한 100%)
  - 실패 시 볼이 남아 있으면 재도전, **누적 2회 실패 시 도망**.
  - 포획 성공: 도감 등록 + 승리 포인트 + 승리 경험치.
- **패배(내 HP 0)** → 기절. 회복약 사용 또는 다음 날 자동 회복까지 배틀 불가.
- **도망치기** 버튼 상시 제공 (패널티 없음).
- **전설 반감 규칙은 채택하지 않음** (쉬운 문제만으로도 시간을 들이면 전설을 잡을 수 있게 유지 — 의도된 결정).
- 내 포켓몬: MVP는 리자몽 고정(maxHP 100, Lv.16 시작). 레벨업: 100XP당 1레벨. (스타팅 선택·진화는 §10 확장)
- **기술 이펙트**: 타입별 템플릿 18종 목표, MVP는 최소 4종(불꽃 파티클 투사, 전기 화면 플래시, 에스퍼 보라 파동, 기본 투사체) + 공통 연출(피격 흔들림, HP바 감속 애니메이션, 몬스터볼 포물선+흔들림 3회). CSS keyframes로 구현 (프로토타입 참조).

### 4.3 야생 탐색 (트랙 A)
- 하루 **3회** (교사 설정으로 조정 가능하게).
- 탐색 → 희귀도 가중 랜덤 출현 → 볼 선택 던지기(배틀과 동일한 성공률 공식) → 2회 실패 시 도망.
- 탐색 횟수는 조우 시점에 차감 (포기해도 반환 없음).

### 4.4 오늘의 실루엣 퀴즈
- 하루 1회. 151마리 중 랜덤 1마리의 실루엣(이미지 CSS `brightness(0)`) + 이름 4지선다.
- 정답: **+100P + 랜덤 볼** (몬스터볼 60% / 슈퍼볼 30% / 하이퍼볼 10%)
- 오답: 위로 보상 **몬스터볼 1개** + 정답 공개. (하루를 기분 나쁘게 시작하지 않게 하는 장치)
- 교사 관리 불필요 — 완전 자동 생성.

### 4.5 문제풀이 (포인트 수입원)
- 출제 중인 문제 은행에서 랜덤 출제. **정답 1개당 +20P**, 하루 **10문제** 한도(교사 설정 가능).
- 오답 시 정답 표시. 모든 풀이 기록은 통계에 집계.

### 4.6 경제 (포인트·상점)
- 수입: 데일리 퀴즈 +100P / 문제풀이 정답 +20P (일 최대 200P) / 배틀 승리 +50~300P / 가입 보너스 500P
- 상점 가격표:

| 아이템 | 가격 | 효과 |
|---|---|---|
| 몬스터볼 | 100P | 기본 포획률 |
| 슈퍼볼 | 300P | 포획률 +15%p |
| 하이퍼볼 | 800P | 포획률 +30%p |
| 마스터볼 | 5,000P | 100% 포획. **1인 1개 한정** (장기 목표·인플레 방지) |
| 상처약 | 100P | 내 포켓몬 HP +50 (기절 상태엔 사용 불가) |
| 회복약 | 250P | 기절 회복 + HP 전부 회복 |

- 기준 환율: 문제 5개 ≈ 몬스터볼 1개. 하루 성실 플레이 시 300~500P 수입이 되도록 유지.

### 4.7 일일 리셋
- 매일 자정(Asia/Seoul) 기준: 퀴즈 1회 / 탐색 3회 / 문제풀이 10문제 초기화, 기절·HP 전부 회복.

## 5. 교사 기능 명세

### 5.1 문제 은행
- 문제 스키마: 문제 내용, 보기 4개, 정답 인덱스, 난이도(쉬움/보통/어려움), 단원 태그(자유 텍스트, 권장 형식 "과목·단원"), 유형(4지선다 기본, OX 선택), 출제 여부(active), 출처(수동/AI/대량), 통계(tries, wrong).
- 목록: 검색, 단원 태그 필터 칩, 난이도 배지, 정답 하이라이트, 문제별 **오답률 표시**, 출제 토글, 수정, 삭제.
- **출제 범위 제어**: active=true인 문제만 배틀·문제풀이에 출제. 태그 단위 일괄 켜기/끄기 제공.

### 5.2 AI 문제 자동 생성 (핵심 기능)
- 입력: 과목 드롭다운(국어, 수학, 사회, 과학, 영어, 도덕, 실과, 체육, 음악, 미술, **진로와직업**, 기타) + 단원·주제(자유 입력) + 학년(초3~중3) + 난이도별 개수 + 추가 지시(선택).
- 파이프라인: 입력 → 프롬프트 조립 → AI 호출(JSON 배열 강제) → 파싱·검증 → **검토 화면**(수정/정답 변경/난이도 변경/선택 체크) → 승인분만 문제 은행 등록.
- **검토 단계는 절대 생략 금지** ("AI 초안 + 교사 최종 결재" 구조).
- 프롬프트 필수 규칙(프로토타입 프롬프트 참조):
  - 오답 보기는 학생들이 실제로 저지르는 흔한 실수·오개념에서 생성
  - 난이도 정의 명시: 쉬움=기본 개념 확인 / 보통=개념 적용 / 어려움=응용·복합·문장제
  - 해당 학년 어휘 사용, 정답 번호 분산, JSON 배열만 출력(코드블록·설명 금지)
- 향후 입력 확장: 교과서 텍스트 붙여넣기 기반 출제, 기존 문제 유형 변형 생성.

### 5.3 AI 연동 아키텍처 (BYOK)
- **기본 제공사: Google Gemini** (무료 등급 존재 — Flash급 모델이면 문제 생성에 충분. 무료 등급은 데이터가 학습에 사용될 수 있으나 문제 생성 용도로는 무해).
- **BYOK(Bring Your Own Key)**: 교사가 설정 화면에서 제공사 선택(Gemini/OpenAI/Claude) + 본인 API 키 등록 + 연결 테스트. 운영자 비용 0원 구조.
- 어댑터 패턴으로 제공사별 호출 모듈 분리(교체 용이).
- 키 보안: 서버 저장 시 암호화 필수, 클라이언트에 타인 키 노출 금지, AI 호출은 서버 경유(키가 브라우저 네트워크 탭에 노출되지 않게).
- 키 미등록 교사도 나머지 기능 전부 사용 가능 — AI 생성 버튼만 비활성 + 안내.
- 앱 내 "5분 키 발급 가이드" 페이지 포함(Google AI Studio 접속 → 로그인 → 키 발급, 스크린샷 포함).
- 남용 방지: 교사당 일일 생성 횟수 제한.

### 5.4 통계
- 문제별: 풀이 횟수, 오답률 (오답률 TOP 10 강조 — "다음 수업에서 다시 다룰 것" 신호).
- 학생별: 정답률, 오늘/누적 풀이 수, 도감 진행도, 최근 접속.
- 학급 전체: 평균 정답률, 참여율.

### 5.5 게임 설정
- 기술=난이도 토글 (기본 ON)
- 하루 야생 탐색 횟수 (기본 3)
- 문제풀이 일일 한도 (기본 10)
- 학생에게 포인트/아이템 선물 (수업 보상용)
- 학생 비밀번호 초기화

## 6. 기술 스택 및 아키텍처 (제안)

- 프론트: React (프로토타입 코드 재사용 극대화). Next.js 권장.
- 백엔드/DB: Supabase(PostgreSQL) 또는 Firebase — 인증·DB·호스팅을 빠르게 구성. AI 호출용 서버 함수(Edge Function/API Route) 필수.
- 배포: Vercel/Netlify + Supabase 조합이면 소규모 학급 운영 기준 무료 티어로 시작 가능.
- PokéAPI 캐싱: 151마리 메타데이터(이름·타입·기술)는 초기 1회 수집해 DB 시드. 이미지는 URL 직참조 + 브라우저 캐시(트래픽 커지면 자체 CDN 캐싱).

### 6.1 데이터 모델 (초안)
```
teachers(id, email, pw_hash, created_at)
classes(id, teacher_id, name, class_code UNIQUE, settings JSONB)
students(id, class_id, nickname, pw_hash, points, inventory JSONB,
         hp, level, xp, day_state JSONB, created_at, UNIQUE(class_id, nickname))
questions(id, class_id, body, options JSONB[4], answer_idx, difficulty,
          tag, type, active, source, created_at)
question_stats(question_id, tries, wrong)            -- 또는 questions에 포함
catches(student_id, pokemon_id, caught_at, method)   -- 도감
answer_logs(id, student_id, question_id, correct, context(battle|solve), created_at)
pokemon_meta(id 1..151, name_ko, type, rarity)       -- 시드 데이터
ai_keys(teacher_id, provider, encrypted_key)
```

## 7. 비기능 요구사항
- 학생 개인정보 무수집 원칙 유지(§2.2). 관련 법규(개인정보보호법) 리스크 최소화 설계.
- 비밀번호 해싱, API 키 암호화, 학급 간 데이터 격리(교사는 자기 학급만 조회).
- 클라이언트 검증 우회 방지: 포인트·아이템·포획 판정은 서버에서 처리(브라우저 콘솔로 포인트 조작 불가하게).
- 응답 속도: 배틀 연출이 생명이므로 문제 출제·판정은 즉답(문제는 배틀 시작 시 프리로드 권장).

## 8. 저작권 유의사항
- 포켓몬 IP는 The Pokémon Company 소유. **교육용·비상업·학급 내부 사용 범위 유지.**
- 유료화·광고 부착·대규모 공개 배포 시 법적 검토 필요. 공유는 "교사들이 각자 자기 학급용으로 설치/사용"하는 형태 권장.
- PokéAPI 자체는 무료지만 상업 사용 시 캐싱 권장 정책 준수.

## 9. 개발 우선순위
1. **M1 — 코어**: 인증(교사/학급코드/학생) + 문제 은행 CRUD + 배틀(문제 연동, 포획까지) + 도감 + 서버 저장
2. **M2 — 경제**: 상점, 데일리 퀴즈, 야생 탐색, 일일 리셋, 문제풀이 탭
3. **M3 — 교사 도구**: AI 생성(BYOK, Gemini 우선), 대량 등록, 통계, 게임 설정, 선물
4. **M4 — 폴리시**: 타입 이펙트 확충, 사운드, 모바일 최적화, 온보딩(첫 접속 튜토리얼)

## 10. 확정된 설계 결정 요약 (변경 시 반드시 본 문서 갱신)
- 오답 = "빗나감" 연출 후 야생의 반격 (반격 데미지는 야생 희귀도로 결정, 선택 기술과 무관 — 어려운 문제 도전을 유도하는 의도)
- 전설 포켓몬에 대한 쉬움 기술 반감 규칙 **미채택**
- 포획: KO 후 볼 던지기, 볼 소모, 실패 가능, 2회 실패 시 도망
- 급소 = 제한시간 앞 1/3 내 정답, 1.5배
- 포인트 단위는 100P 스케일 (데일리 100P 기준, 가격표 §4.6)
- 학생 가입은 학급 코드 + 닉네임 + 비밀번호만
- AI는 Gemini 기본 + BYOK, 검토·승인 후 등록
- 대량 등록 구분자: `문제 | 보기1 | 보기2 | 보기3 | 보기4 | 정답번호(1~4) | 난이도(쉬움/보통/어려움) | 태그(선택)`

## 11. 향후 확장 아이디어 (백로그)
- 스타팅 포켓몬 선택(이상해씨/파이리/꼬부기) + **진화 시스템**(PokéAPI evolution-chain, 레벨 도달 시 진화 연출) — 애착·리텐션 핵심
- **오답 재출현(간격 반복)**: 배틀에서 틀린 문제를 학생별 오답 풀에 넣어 재출제
- 2세대 해금 (1세대 도감 완성 보상)
- 포켓몬 희귀도-문제 난이도 연동 심화, 타입 상성 학습 요소
- 학급 리더보드, 협동 이벤트(반 전체 목표 달성 시 전설 출현율 업)
- 교사용 AI 입력 확장: 교과서 붙여넣기 출제, 문제 변형 생성
- 단답형 문제 유형, 이미지 문제

---
*작성일: 2026-07-07 · 근거: 프로토타입 1~4단계 및 설계 대화. 이 문서와 프로토타입 4종을 함께 Claude Code에 전달하여 M1부터 착수할 것.*
